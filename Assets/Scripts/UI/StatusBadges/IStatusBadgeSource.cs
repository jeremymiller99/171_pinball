// Generated by Claude Code (claude-opus-5) for jjmil on 2026-07-28.

using UnityEngine;

/// <summary>
/// One row of the status readout that sits under a component or ball: an icon and
/// a short label. Sources hand these back on demand rather than pushing updates,
/// so a source can appear and disappear (Fire) or sit at zero forever (a Charge
/// requirement) without the display needing to know which is which.
/// </summary>
public readonly struct StatusBadgeInfo
{
    public readonly Sprite Icon;
    public readonly string Label;
    public readonly Color Tint;

    /// <summary>
    /// Left-to-right position in the row. Kept as a number rather than relying on
    /// component order so a runtime-added <see cref="FireStatus"/> does not land
    /// wherever <c>AddComponent</c> happened to put it.
    /// </summary>
    public readonly int SortOrder;

    public StatusBadgeInfo(Sprite icon, string label, Color tint, int sortOrder)
    {
        Icon = icon;
        Label = label;
        Tint = tint;
        SortOrder = sortOrder;
    }
}

/// <summary>
/// Implemented by anything with a status worth showing under its object — Fire,
/// Charge, a Cannon's fuse. <see cref="StatusBadgeDisplay"/> discovers these with
/// <c>GetComponents</c>, so a new keyword only has to implement this to appear;
/// nothing about the display needs editing.
/// </summary>
public interface IStatusBadgeSource
{
    /// <summary>
    /// Returns false when this source has nothing to show right now, which hides
    /// its row. Fire returns false while unlit; a Charge requirement stays visible
    /// at zero so the player can see what the component wants.
    /// </summary>
    bool TryGetStatusBadge(out StatusBadgeInfo info);
}
